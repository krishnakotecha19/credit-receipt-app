# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=credit_reciept
DB_USER=postgres
DB_PASSWORD=your_password_here

# Ollama (local LLM)
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5vl:3b
# Fallback text-only model (used if VLM fails or not enough RAM)
OLLAMA_TEXT_MODEL=qwen2.5:3b
